mutation LocationMutation($input: LocationInput!) {
    location(input: $input) {
        id
        name
        building
        city
        country
        mapsLink
        postalCode
        street
        streetNumber
        website
    }
}
